name: my frist github actions
on: 
  push:                               # master Branch에서 push 이벤트가 일어났을 때만 실행
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-18.04
    steps:
      - name: 레포지터리 체크아웃!  # 레포지토리 체크아웃
        uses: actions/checkout@master

      - name: 에코 테스트    # 의존 파일 설치
        run: echo "Hello there"